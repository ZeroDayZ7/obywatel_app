import 'package:flutter/material.dart';
import '../spacing.dart';

class ScreenMargins {
  static const all = EdgeInsets.all(Spacing.lg);  // 16px
  static const horizontal = EdgeInsets.symmetric(horizontal: Spacing.xxl); // 24px
  static const vertical = EdgeInsets.symmetric(vertical: Spacing.md);      // 12px
}
