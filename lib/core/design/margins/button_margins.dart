import 'package:flutter/material.dart';
import '../spacing.dart';

class ButtonMargins {
  static const small = EdgeInsets.symmetric(horizontal: Spacing.md, vertical: Spacing.sm);
  static const medium = EdgeInsets.symmetric(horizontal: Spacing.lg, vertical: Spacing.md);
  static const large = EdgeInsets.symmetric(horizontal: Spacing.xxl, vertical: Spacing.lg);
}
