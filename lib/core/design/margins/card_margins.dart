import 'package:flutter/material.dart';
import '../spacing.dart';

class CardMargins {
  static const all = EdgeInsets.all(Spacing.lg); // 16px
  static const small = EdgeInsets.all(Spacing.sm); // 8px
}
