import 'package:flutter/material.dart';

import '../tokens/spacing.dart';

class InputMargins {
  static const vertical = EdgeInsets.symmetric(vertical: Spacing.sm);
  static const horizontal = EdgeInsets.symmetric(horizontal: Spacing.md);
  static const all = EdgeInsets.all(Spacing.md);
}
