import 'package:flutter/material.dart';

class EmploymentMapPage extends StatelessWidget {
  const EmploymentMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Employment Map Page'));
  }
  }

